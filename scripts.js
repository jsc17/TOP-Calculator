let first, second, operator;

function add(first, second) {
  return first + second;
}

function subtract(first, second) {
  return first - second;
}

function multiply(first, second) {
  return first * second;
}

function divide(first, second) {
  if (second == 0) return "Divide by Zero error";
  return first / second;
}

function clear() {}

function equals() {}

function onClickNumber() {}

function onClickOperator() {}
